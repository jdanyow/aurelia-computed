'use strict';

System.register(['./aurelia-computed'], function (_export, _context) {
  "use strict";

  return {
    setters: [function (_aureliaComputed) {
      var _exportObj = {};

      for (var _key in _aureliaComputed) {
        if (_key !== "default") _exportObj[_key] = _aureliaComputed[_key];
      }

      _export(_exportObj);
    }],
    execute: function () {}
  };
});